{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Build and Run",
            "type": "shell",
            "command": "gcc *.c -o game -lraylib -lm && ./game",
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}